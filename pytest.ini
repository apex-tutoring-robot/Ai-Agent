[pytest]
; src/ modules import each other as top-level packages (e.g. "from session
; import Session", "from profiles.profile_manager import ProfileManager"),
; matching how main.py itself is run - this puts src/ on sys.path the same
; way so tests can import them without a src. prefix.
pythonpath = src
testpaths = tests/unit
