"""Automated unit tests (pytest) for src/ modules - not to be confused with
the hardware test scripts one level up in tests/, which need real audio
devices/displays and are meant to be run manually, not in CI."""
