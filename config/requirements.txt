# ==============================================================================
# SYSTEM DEPENDENCIES - INSTALL THESE FIRST (Raspberry Pi / Linux)
# ==============================================================================
# Before running 'pip install -r requirements.txt', install these system packages:
#
# sudo apt-get update && sudo apt-get install -y \
#     libcap-dev \
#     swig \
#     python3-libcamera \
#     portaudio19-dev \
#     libasound2-dev \
#     libportaudio2 \
#     libportaudiocpp0 \
#     libspeexdsp-dev \
#     python3-dev \
#     libopenblas-dev \
#     liblapack-dev \
#     ffmpeg
#
# These are required for PyAudio, webrtcvad, picamera2, and openwakeword to compile.
# ==============================================================================

# Azure AI Services
azure-cognitiveservices-speech==1.40.0
azure-appconfiguration>=1.6.0
openai>=1.12.0

# NeMo Guardrails for topic control and child safety
nemoguardrails>=0.9.0

# LangChain integration
langchain-openai>=0.1.0
langchain-core>=0.1.0

# Audio Processing
# IMPORTANT: Requires portaudio19-dev and libasound2-dev (see system deps above)
pyaudio==0.2.14
webrtcvad-wheels>=2.0.11  # Drop-in replacement for webrtcvad, Python 3.13 compatible

# Configuration & Utilities
python-dotenv==1.0.1

# Async Support
aiofiles==24.1.0

# Build Tools
build
cython

# Numeric & Vision
numpy>=2.1.0
sounddevice==0.4.6
opencv-python>=4.10.0

# HTTP
httpx==0.27.0

# Typing
typing-extensions==4.15.0

# Wake Word Detection
openwakeword

# Acoustic Echo Cancellation
# IMPORTANT: Requires libspeex-dev and libspeexdsp-dev (see system deps above)
speexdsp

# Camera
# IMPORTANT: Requires libcap-dev (see system deps above)
picamera2
