# Azure App Configuration (bootstrap credential — the only secret stored on the device)
# Use the read-only connection string from: Azure Portal → App Configuration → Access keys
# When set, AZURE_SPEECH_KEY, AZURE_OPENAI_API_KEY, and related secrets are fetched
# from Azure at startup and no longer need to be set below.
# Optional: set AZURE_APP_CONFIG_LABEL to scope to a specific label (e.g. "production")
AZURE_APP_CONFIG_CONNECTION_STRING=
AZURE_APP_CONFIG_LABEL=

# Azure Speech Services
AZURE_SPEECH_KEY=your_azure_speech_key
AZURE_SPEECH_REGION=your_azure_speech_region

# Azure OpenAI
AZURE_OPENAI_API_KEY=your_azure_openai_key
AZURE_OPENAI_ENDPOINT=your_azure_openai_endpoint
AZURE_OPENAI_DEPLOYMENT=your_azure_openai_deployment
AZURE_OPENAI_API_VERSION=your_azure_openai_api_version

# Picovoice Wake Word
PICOVOICE_ACCESS_KEY=your_picovoice_access_key

# openwakeword
WAKE_WORD_MODEL_PATH=.venv/lib/python3.13/site-packages/openwakeword/resources/models/hey_jarvis_v0.1.onnx
WAKE_WORD_MODEL="hey_jarvis"
WAKE_WORD_THRESHOLD=0.5
WAKE_WORD_VAD_THRESHOLD=0.5

# USB Audio Device Configuration
# Run 'python -c "import pyaudio; p = pyaudio.PyAudio(); [print(f\"{i}: {p.get_device_info_by_index(i)['name']}\") for i in range(p.get_device_count())]"'
# to list available audio devices and their indices
AUDIO_INPUT_DEVICE_INDEX=1
AUDIO_OUTPUT_DEVICE_INDEX=1

# Audio Parameters
SAMPLE_RATE=16000
CHANNELS=1
CHUNK_SIZE=512
VAD_AGGRESSIVENESS=3
SILENCE_TIMEOUT_MS=1500

# Conversation Settings
MAX_CONVERSATION_HISTORY=20
SYSTEM_PROMPT_PATH=./config/system_prompt.txt
CONVERSATION_IDLE_TIMEOUT_SECONDS=10

# TTS Settings
TTS_VOICE=en-US-JennyNeural
TTS_SPEECH_RATE=1.0

# Logging
LOG_LEVEL=INFO

# AEC & Full-Duplex Tuning
AEC_DELAY_CHUNKS=12
AEC_HARDWARE_LATENCY_MS=150
ENABLE_INTERRUPTION=true
FACE_ENABLED=false

# NeMo Guardrails
GUARDRAILS_CONFIG_PATH=config/guardrails
GUARDRAILS_ENABLED=true

BARGE_IN_ENERGY_THRESHOLD=2500
BARGE_IN_CHUNKS_NEEDED=3